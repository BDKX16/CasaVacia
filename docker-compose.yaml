services:
  nextjs:
    container_name: casa-vacia
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - 3123:3000 
    environment:
      - NODE_ENV=production
      # Importante para que Next.js escuche en todas las interfaces
      - HOSTNAME=0.0.0.0

